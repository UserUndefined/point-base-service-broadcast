<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
  <StackLayout>
    <Label text="Start Tracking" class="title"/>
    <Button text="Every Hour" tap="{{ onTap }}" />
    <!--Label text="{{ message }}" class="message" textWrap="true"/-->
  </StackLayout>
</Page>